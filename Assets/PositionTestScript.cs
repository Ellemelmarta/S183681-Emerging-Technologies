using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PositionTestScript : MonoBehaviour {

	//private transform 

	//void testingsomething()
	//{
		//this needs to get the position of the attachment pinky and then add on a certain number to the x position so it sits ever so slightly to the right
		//or you need to find a way to make the position of the anchor be there to begin with but not sure how to do this
		//could also make the arm the place which stores all the anchors as it would just be easier but this is the lazy way of doing it
	//}
}
